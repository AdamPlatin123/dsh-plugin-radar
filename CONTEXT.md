# 术语表（CONTEXT.md）

本仓库域模型的唯一词汇来源。只记术语与边界，不记实现细节。

## 判定（verdict）

一个插件在真实运行环境的兼容性结论。按**事实来源**分两种，清单中必须可区分：

- **实测判定**：雷达在 k8s 容器中对插件真实安装运行得出的结论（✅/❌）。由雷达流转写入，是权威事实。
- **自报判定**：登记作者在 PR 自检清单中声明、但雷达尚未复测的结论（✅(自报)）。是登记声明，不是实测事实；雷达流转后自动升格为实测判定（标记消失）。

判定列同时是「待测」的入口标记：新登记默认待测，等待实测判定。

（决议于 2026-08-27 来源标记制 grilling，适用 PLUGINS.md 判定列与渲染产物。）

## 三仓角色（清单仓 / 管线仓 / 引擎镜像仓）

雷达生态的三个仓库（2026-09-27 修订，P1 全量重构后口径）：

- **清单仓**（AdamPlatin123/awesome-dsh-plugins）：社区登记 PR 的合并地，持有权威登记表（PLUGINS.md），面向读者的门面。
- **管线仓**（dsh-external/awesome-dsh-plugins）：快照生产与渲染源，雷达数据权威。
- **引擎镜像仓**（AdamPlatin123/dsh-plugin-radar，即本仓）：引擎开源副本 + 站点宿主 + 管线数据镜像。数据流：管线仓→本仓（org→mirror 同步）→GitHub Pages 站点；渲染与导出经 canonical 单一事实源（见 docs/radar/architecture.md）。

数据单向流：管线仓→清单仓、管线仓→本仓；登记表只在清单仓。（2026-08-27 首次正名取代「主仓/镜像」称呼；2026-09-27 增补第三仓角色。）

## 登记清单与全量索引

两份清单是不同概念：**登记清单**（PLUGINS.md）= 人工策展的插件登记表，条目由社区 PR 加入；**全量索引**（PLUGINS-ALL.md）= 雷达发现轨与登记轨的并集快照渲染，条目来自自动发现。登记是全量的子集来源之一，两者不可互相替代。

## 快照基线与窗口

**快照**是管线仓产出的雷达状态切面（节奏随增量自适应，现为日 1–3 份）。**基线（baseline）**是全部历史快照并集的一次性固化，保证老仓发现永不丢失；**窗口**是清单仓保留的近期快照（约 30 天，审计用）。渲染 = 基线 ⊕ 窗口内快照；每季度重固化基线。

## 版本化判定

带版本锚的兼容结论：判定 ×（插件版本 · DSH 版本），呈现为静态文本锚（如 ✅@v1.2·rc.8）。版本锚只回答「结论在哪个版本下成立」，不参与活跃度淘汰判定。

## 档案区

全量清单尾部收纳陈旧低星仓的分组。入档 = 无推送达到窗口阈值 且 star<5（两项缺一不可；与实测新旧无关）。窗口随项目阶段自适应：存量清理期 2 周，常态化 6 个月。数据永不物理删除（保留于快照基线）；star 回升或恢复推送即出档重测。

## canonical（单一事实源）

`generated/current/canonical.json`（radar-canonical/v1）：全部数据源（快照 ⊕ 基线 ⊕ 登记轨 ⊕ 定位/描述缓存）归并后的唯一内部事实源。渲染（PLUGINS-ALL/catalog）、导出（latest/plugins-all）、站点烘焙三端同源消费，Markdown 不再被反向解析。（2026-09-27 P1 数据层收敛，ADR-0004。）

## 补采 sidecar（plugins-enrich）

`data/plugins-enrich.json`（dsh-enrich/v1）：GraphQL 补采的增量元数据（pushed_at/作者头像/topics/license/language/README 首图），与全量清单正交、键同 repo、字段只增不删。站点详情页与画廊的数据源。（2026-09-27 P6，ADR-0005。）
